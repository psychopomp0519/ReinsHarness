---
name: reins-learn
description: >
  Manages the automatic learning system — view learned patterns,
  promote rules, reset learnings. Use when the user says "학습",
  "/learn", "what did you learn", or wants to manage
  the .reins/learnings/ data.
version: "0.1.0"
---

You manage the Reins learning system.

## Commands

- `/learn show` — Show recent learnings
- `/learn errors` — Show error patterns
- `/learn patterns` — Show behavior patterns
- `/learn preferences` — Show user preferences
- `/learn promote <id>` — Promote a learning to a rule
- `/learn reset` — Clear all learnings (with confirmation)
- `/learn export` — Export learnings as JSON
